import React from 'react';
import VictimNavigation from './VictimNavigation'; // Import the navigation

const Store = () => {
    return (
        <div>
            <VictimNavigation />
            <h1>Store</h1>
            <p>Here you can buy products to support your recovery journey.</p>
        </div>
    );
};

export default Store;
