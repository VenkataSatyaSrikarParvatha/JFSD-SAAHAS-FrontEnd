// src/components/visitor/Requirements.js
import React from 'react';
import Navigation from './Navigation'; // Import the Navigation component
//import './Requirements.css'; // Create this CSS file for styling

const Requirements = () => {
    return (
        <div className="requirements-container">
            <Navigation />
            <h1>Requirements for Counselors, Doctors, and Advisors</h1>
            <p>Here are the requirements necessary for those roles...</p>
        </div>
    );
};

export default Requirements;
